import React, { useState, useEffect } from 'react'
import axios from 'axios'

const Weather = ({ city }) => {

    //fetch weather data using API and display it
    return (
        <>

        </>
    )
}

export default Weather
