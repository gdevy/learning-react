import React, { useState } from 'react'
import Country from './Country'

const CountryItem = ({ country }) => {

    //return a Country, manage button expansion state
    return(
        <>
        </>
    )
}

export default CountryItem
